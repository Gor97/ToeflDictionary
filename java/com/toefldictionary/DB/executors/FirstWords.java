package com.toefldictionary.DB.executors;import android.content.Context;import android.util.Log;import com.toefldictionary.DB.TOEFL_DB;import com.toefldictionary.DB.executors.objects.Type;import com.toefldictionary.DB.executors.objects.Word;import java.util.ArrayList;/** * Created by Gor on 01-May-16. */public class FirstWords {    public static boolean finished = false;    public static void addingFirstWords(Context context)    {        TOEFL_DB db = TOEFL_DB.getInstance(context);        ArrayList<Type> types = new ArrayList<>();        Type t = new Type("Basic Words"); types.add(t);        t = new Type("My Words"); types.add(t);        t = new Type("Food Crops"); types.add(t);        t = new Type("Disaster"); types.add(t);        t = new Type("Evolution and Migration"); types.add(t);        t = new Type("Petroleum Alternatives"); types.add(t);        t = new Type("Time Efficiency"); types.add(t);        t = new Type("Ancient Life"); types.add(t);        t = new Type("Computers"); types.add(t);        t = new Type("Energy"); types.add(t);        t = new Type("Memory"); types.add(t);        t = new Type("Spirituality"); types.add(t);        t = new Type("Illness"); types.add(t);        t = new Type("Surgery"); types.add(t);        t = new Type("Ghosts"); types.add(t);        t = new Type("Anthropology"); types.add(t);        t = new Type("Social Inequality"); types.add(t);        t = new Type("Expertise"); types.add(t);        t = new Type("Military Operations"); types.add(t);        t = new Type("War and Conquest"); types.add(t);        t = new Type("History"); types.add(t);        t = new Type("Financial Systems"); types.add(t);        t = new Type("Wealth and Social Class"); types.add(t);        t = new Type("Personal Property"); types.add(t);        t = new Type("Employment"); types.add(t);        t = new Type("International Trade"); types.add(t);        t = new Type("Politics"); types.add(t);        t = new Type("A Reasonable Doubt"); types.add(t);        t = new Type("The Police"); types.add(t);        t = new Type("Investigating Crimes"); types.add(t);        t = new Type("Government Corruption"); types.add(t);        t = new Type("Crimes at Sea"); types.add(t);        t = new Type("The War on Drugs"); types.add(t);        t = new Type("Family Relationships"); types.add(t);        t = new Type("Friendship"); types.add(t);        t = new Type("Passion"); types.add(t);        t = new Type("Negative Emotions"); types.add(t);        t = new Type("Social Rebels"); types.add(t);        t = new Type("Painting and Sculpture"); types.add(t);        t = new Type("The Written Word"); types.add(t);        t = new Type("Entertainment"); types.add(t);        t = new Type("Risky Fashions"); types.add(t);        for(Type type:types) {            db.addType(type);        }        /////////////////////////////////////////////////////////////////////////////////////////////////////                                            //  FOOD CROPS  //        /////////////////////////////////////////////////////////////////////////////////////////////////////        Word w = new Word("abandon", "Թողնել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 2);        Word s = new Word("leave", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(), s.getId());        s = new Word("give up", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(),s.getId());        s = new Word("forsake", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(), s.getId());        s = new Word("jilt", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(), s.getId());        Word a = new Word("hold", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        a = new Word("defend", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        a = new Word("maintain", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        w = new Word("adversely", "Բացասաբար");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 2);        //*adversely        s = new Word("unfavorably", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(), s.getId());        s = new Word("harshly", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(),s.getId());        a = new Word("positively", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        a = new Word("agreeably", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        a = new Word("advantageously", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        //*        w = new Word("aggregate", "Ժողովել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 2);        //*aggregate        s = new Word("collect", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(), s.getId());        s = new Word("amass", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(),s.getId());        s = new Word("sum", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(),s.getId());        s = new Word("amount", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(),s.getId());        a = new Word("divide", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        a = new Word("break up", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        a = new Word("disperse", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        //*        w = new Word("cultivation", "Աճեցում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 2);        //*cultivation        s = new Word("culture", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(), s.getId());        s = new Word("breeding", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(),s.getId());        s = new Word("farming", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(),s.getId());        a = new Word("ignorance", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        a = new Word("coarseness", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        a = new Word("destruction", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        //*        w = new Word("fertilize", "Պարարտացնել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 2);        w = new Word("intensify", "Ուժեղացնել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 2);        w = new Word("irrigation", "Ոռոգում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 2);        w = new Word("obtain", "Ստանալ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 2);        w = new Word("photosynthesis", "Ֆոտոսինթեզ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 2);        w = new Word("precipitation", "Երկրին հասնող ջուր");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 2);        /////////////////////////////////////////////////////////////////////////////////////////////////////                                                //  DISASTER //        /////////////////////////////////////////////////////////////////////////////////////////////////////        w = new Word("anticipate", "Կանխազգալ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 3);        //*anticipate        s = new Word("await", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(), s.getId());        s = new Word("except", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(),s.getId());        s = new Word("predict", "");        s = db.addWord(s);        db.addWordSynonym(w.getId(),s.getId());        a = new Word("unforeseen", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        a = new Word("doubt", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        a = new Word("unexpected", "");        a = db.addWord(a);        db.addWordAntonym(w.getId(), a.getId());        //*        w = new Word("catastrophic", "Աղետալի");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 3);        w = new Word("collide", "Բախվել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 3);        w = new Word("eruption", "Ժայթքում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 3);        w = new Word("famine", "Սով");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 3);        w = new Word("flood", "Ջրհեղեղ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 3);        w = new Word("impact", "Ազդեցություն");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 3);        w = new Word("persevere", "Պնդել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 3);        w = new Word("plunge", "Սուզվել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 3);        w = new Word("unleash", "Սանձազերծել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 3);        /////////////////////////////////////////////////////////////////////////////////////////////////////                                         //   Evolution and Migration //        /////////////////////////////////////////////////////////////////////////////////////////////////////        w = new Word("adapt", "Հարմարեցնել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 4);        w = new Word("diverse", "Բազմազան");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 4);        w = new Word("evolve", "Զարգանալ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 4);        w = new Word("feature", "Առանձնահատկություն");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 4);        w = new Word("generation", "Սերունդ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 4);        w = new Word("inherent", "Հատուկ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 4);        w = new Word("migration", "Գաղթ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 4);        w = new Word("physical", "Ֆիզիկական");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 4);        w = new Word("process", "Ընթացք");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 4);        w = new Word("survive", "Փրկվել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 4);        /////////////////////////////////////////////////////////////////////////////////////////////////////                                             //   Petroleum Alternatives   //        /////////////////////////////////////////////////////////////////////////////////////////////////////        w = new Word("constraint", "Հարկադրանք");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 5);        w = new Word("contamination", "Աղտոտում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 5);        w = new Word("deplete", "Սպառել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 5);        w = new Word("dispose of", "Դեն նետել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 5);        w = new Word("elementally", "Հիմնականում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 5);        w = new Word("emission", "Արտանետումներ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 5);        w = new Word("extinction", "Ոչնչացում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 5);        w = new Word("reservoir", "Ամբար");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 5);        w = new Word("shrink", "Չափերով նեղանալ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 5);        w = new Word("stable", "Կայուն");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 5);        /////////////////////////////////////////////////////////////////////////////////////////////////////                                     //   Time Efficiency   //        /////////////////////////////////////////////////////////////////////////////////////////////////////        w = new Word("adjust", "Հարմարեցնել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 6);        w = new Word("arbitrary", "Ինքնակամ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 6);        w = new Word("denominator", "Հայտարար");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 6);        w = new Word("exponentially", "շատ արագ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 6);        w = new Word("infinitesimal", "Անվերջ փոքր");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 6);        w = new Word("maximize", "Հասցնել առավելագույնի");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 6);        w = new Word("parallel", "Զուգահեռ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 6);        w = new Word("proportion", "Համամասնություն");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 6);        w = new Word("rate", "Տոկոսադրույք");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 6);        w = new Word("sequence", "Հերթականություն");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(), 6);        /////////////////////////////////////////////////////////////////////////////////////////////////////                                        //   Ancient Life   //        /////////////////////////////////////////////////////////////////////////////////////////////////////        w = new Word("accuracy", "Ճշգրտություն");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  7);        w = new Word("adjacent", "Հարակից");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  7);        w = new Word("compress", "Ճզմել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  7);        w = new Word("feasibly", "Իրագործելի");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  7);        w = new Word("gut", "Դատարկել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  7);        w = new Word("integrally", "Անբաժանելի");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  7);        w = new Word("overlap", "Մասամբ համընկնել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  7);        w = new Word("retain", "Պահպանել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  7);        w = new Word("seep", "Հոսել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  7);        w = new Word("structure", "Կառուցվածք");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  7);        /////////////////////////////////////////////////////////////////////////////////////////////////////                                            //  Computers   //        /////////////////////////////////////////////////////////////////////////////////////////////////////        w = new Word("circulate", "Շրջանառել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  8);        w = new Word("corrode", "Մաշել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  8);        w = new Word("derive", "Ստանալ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  8);        w = new Word("detection", "Հայտնաբերում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  8);        w = new Word("expeditiously", "Արագ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  8);        w = new Word("implement", "Կատարել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  8);        w = new Word("innovative", "Նորարարական");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  8);        w = new Word("installation", "Տեղակայում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  8);        w = new Word("maintenance", "Պահպանում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  8);        w = new Word("simulation", "Մոդելավորում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  8);        /////////////////////////////////////////////////////////////////////////////////////////////////////                                            //  Energy   //        /////////////////////////////////////////////////////////////////////////////////////////////////////        w = new Word("combustion", "Այրում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  9);        w = new Word("component", "Բաղադրիչ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  9);        w = new Word("convey", "Հաղորդել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  9);        w = new Word("discretely", "Առանձին");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  9);        w = new Word("nucleus", "Միջուկ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  9);        w = new Word("permeate", "Տարածվել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  9);        w = new Word("rotate", "Պտտել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  9);        w = new Word("solar", "Արեգակնային");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  9);        w = new Word("source", "Աղբյուր");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  9);        w = new Word("trigger", "Ձգան");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  9);        /////////////////////////////////////////////////////////////////////////////////////////////////////                                                //  Memory   //        /////////////////////////////////////////////////////////////////////////////////////////////////////        w = new Word("acquisition", "Ստացում");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  10);        w = new Word("anomaly", "Անկանոնություն");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  10);        w = new Word("consciously", "Գիտակցաբար");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  10);        w = new Word("degrade", "Նվազեցնել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  10);        w = new Word("gap", "Թերություն");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  10);        w = new Word("indisputable", "Անվիճելի");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  10);        w = new Word("intervene", "Միջամտել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  10);        w = new Word("intuitively", "Ինտուիտիվ");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  10);        w = new Word("recede", "Իջնել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  10);        w = new Word("retrieve", "Վերականգնել");        w = db.addWord(w);        db.addWordType(w.getId(), 1);        db.addWordType(w.getId(),  10);        finished = true;    }}